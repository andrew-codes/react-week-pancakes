'use strict';

import React from 'react';

export default React.createClass({
  render() {
    return (
      <main>
        <h1>These are not the issues you are looking for...</h1>
      </main>
    );
  }
});
